using UnityEngine;

namespace Objects.Interfaces
{
    public class IBarrelAnimator : MonoBehaviour
    {
        // Start is called before the first frame update
        void Start()
        {
        
        }

        // Update is called once per frame
        void Update()
        {
        
        }
    }
}
